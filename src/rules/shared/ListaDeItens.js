const inventario = {
    pao: [1],
    bolo: [2],
    cookie: [3]
}

export default class ListaDeItens {
    constructor(
        pao,
        bolo,
        cookie

    ){
        this.pao = pao,
        this.bolo = bolo,
        this.cookie = cookie
    }
}